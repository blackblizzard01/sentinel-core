[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
log_cli = true
log_cli_level = INFO
markers =
    smokerun: fast smoke tests that verify core infrastructure without a live server
